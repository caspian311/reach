<div class="title"> 
  <%= @title %>: <%= @map_name %>
</div> 
<div class="content"> 

Average player effectiveness per map.<br />
What is <%= link_to "player effectiveness", "/player_effectiveness_info#index" %>?

<br /><br />

<strong>Map:</strong>
<select onchange="window.location='/player_effectiveness/' + this.value">
<option value=""></option>
<% @maps.each do |map| %>
   <option <%= (map.name == @map_name) ? "selected='selected'": "" %> value="<%= map.name %>"><%= map.name %></option>
<% end %>
</select>

<br /><br />

<table>
<tr>
   <th>Player Name</th>
   <th>Map</th>
   <th>Effectiveness</th>
   <th>Number of Games</th>
</tr>
<% @player_stats.each do |player_stat| %>
<tr class="<%= cycle("regular", "alternate") %>">
   <td><%= player_stat.player_name %></td>
   <td><%= player_stat.map %></td>
   <td><%= number_with_precision(player_stat.effectiveness, :precision => 2) %></td>
   <td><%= player_stat.number_of_games %></td>
</tr>
<% end %>
</table>

</div>
